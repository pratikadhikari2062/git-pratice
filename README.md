# git init
git initlized

# git clone url
repo clone

# git add
add all changed file 

# git status
look into list of changed files
# git commit -m 'message'
commiting message to chake file

# git chakeout naem
swich to branch

# git chakeout -b name
create a new branch

# git branch
list if branches

# git branch -d name
delet branch

# git restore
reset all fole

# git push origin main
Push the main branch to the remote origin:

# git push -u origin  name
Push a branch that you've never pushed before:

# git push --tags
Push tags:
